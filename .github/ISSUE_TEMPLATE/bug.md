---
name: Bug report
about: cast-doctor reporting something wrong, or missing a real problem
labels: bug
---

## What happened

<!-- Brief description -->

## What you expected

<!-- The correct check result, or the missed problem -->

## To reproduce

```bash
# Exact commands + relevant settings.json snippet (redact secrets)
```

## Environment

- cast-doctor version: `cast-doctor version`
- OS: `sw_vers` (macOS) or `lsb_release -a` (Linux)
- bash: `bash --version | head -1`
- python3: `python3 --version`
- sqlite3: `sqlite3 --version`

## Output

```
# Paste full `cast-doctor` (or `cast-doctor --json`) output
```
